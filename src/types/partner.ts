export interface Partner {
    id: number
    name: string
    email:string
    age: number
    course: string
    year: number
    bio: string
    interests: string[]
    photos: string[]
    gender:string
}
